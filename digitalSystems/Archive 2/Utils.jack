class Utils {
    constructor Utils new() {
        var int x; // placeholder so the class won't crash the game
        return this;
    }

    // Returns the remainder part of x/y
    function int mod(int x, int y) {
        var int q;
        
        let q = Math.divide(x, y);
        return x - (q*y);
    }

    // returns x in the power of n
    function int pow(int x, int n) {
        var int result;
        let result = 1;
        if (n = 0) {
            return result;
        }
        // result might overflow
        while (n > 0) {
            let result = result * x;
            let n = n - 1;
        }
        return result;
    }
}